import { Sorting } from 'types/types';

const paginationLimits = ['20', '60', '80', '100', '200'];
const sortings: Sorting[] = [Sorting.ASC, Sorting.DESC, Sorting.DEFAULT];

export { paginationLimits, sortings };
